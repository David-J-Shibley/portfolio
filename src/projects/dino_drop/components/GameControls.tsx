import React from "react";

const GameControls: React.FC = () => {
  return (
    <div>
      {/* TODO: Game controls (pause, restart, etc.) */}
      <p>Game Controls</p>
    </div>
  );
};

export default GameControls; 